# Session Worklog Writer Skill 实现计划

## Context

用户需要一个 skill，在每次 session 工作结束时自动生成文档并更新项目信息。同时，在开启新对话时能够快速获取项目上下文，且 token 消耗要尽可能少。

### 用户需求
1. **Session 结束时自动执行**：生成需求文档、工作日志，并更新相关文档（如 README）
2. **新对话快速上下文**：通过自动引用最近的工作日志快速了解项目状态
3. **Token 优化**：避免每次对话自动加载大量内容，采用按需加载策略

### 项目现状
- 已有工作日志目录：`docs/worklog/`，采用标准 7 章节格式
- 已有需求文档目录：`docs/plans/`，采用标准需求文档格式
- 工作日志命名：`YYYY-MM-DD-功能模块名称.md`
- 需求文档命名：`YYYY-MM-DD-功能模块需求.md`

## 实现方案

### 一、创建 Skill: `context-keeper`

**触发方式：** `/context-keeper`

**功能：** 对话上下文的保存和加载，支持在多个对话之间保持连续性

#### 交互流程

**Step 1: 用户调用命令**
```
用户：/context-keeper
```

**Step 2: Skill 展示菜单**
```
Context Keeper - 对话上下文管理工具

这个工具帮助你在多个对话之间保持项目上下文的连续性。

功能：
1. 保存当前对话的上下文（生成需求文档、工作日志、更新相关文档）
2. 加载最近的上下文（快速了解项目当前状态）

请选择操作（输入数字）：
1 - 保存上下文
2 - 加载上下文
```

**Step 3: 用户选择操作**
```
用户：1  （或 2）
```

**Step 4: 执行对应操作**

#### 操作 1：保存上下文

**工作流程：**
1. 分析对话历史和 git 变更
2. 生成需求文档（`docs/plans/YYYY-MM-DD-HHmm-[功能模块]需求.md`）
3. 生成工作日志（`docs/worklog/YYYY-MM-DD-HHmm-[功能模块].md`）
4. 在工作日志末尾添加"快速摘要"章节（100-150 tokens）
5. 更新相关文档（README.md 等）
6. 简单通知用户结果

**支持同一对话多次保存：**
- 文件名包含时间戳（HHmm）以区分同一天的多次保存
- 例如：`2026-04-03-1430-Solutions页面导航栏添加.md`
- 每次保存都是独立的快照，不会覆盖之前的保存

**快速摘要格式：**
```markdown
## 快速摘要（用于下次对话）

**完成：** [一句话描述完成的工作]
**问题：** [遇到的主要问题及解决方案，如果有]
**下一步：** [下一步计划]
**文件：** [新增/修改的关键文件]
```

#### 操作 2：加载上下文

**工作流程：**
1. 查找最近的工作日志文件（`docs/worklog/` 中最新的）
2. 只读取"快速摘要"章节（约 100-150 tokens）
3. 简洁地告知用户当前项目状态
4. 询问用户是否需要查看完整的工作日志或需求文档

### 二、文档生成逻辑

#### 需求文档结构
基于现有格式（参考 `docs/plans/2026-03-30-最佳实践三层级页面需求.md`）：
- 标题 + 元信息（创建日期、项目、功能模块）
- 一、需求概述
- 二、功能结构/页面层级结构
- 三、数据来源
- 四、技术要求
- 五、设计要求
- 六、验证标准
- 七、参考资料

**信息来源：** 从对话历史中提取用户需求、技术讨论、设计要求等

#### 工作日志结构
基于现有格式（参考 `docs/worklog/2026-04-03-solutions页面导航栏添加与主题适配.md`）：
- 标题：`# 工作日志 - YYYY-MM-DD`
- 项目信息：`## 项目：ClawPower 官网 - [功能模块]`
- 一、任务概述
- 二、完成的工作
- 三、遇到的问题和解决方案
- 四、创建/修改的文件清单
- 五、技术栈/技术决策
- 六、下一步工作/总结
- 七、备注（可选）
- **快速摘要（新增）**

**信息来源：**
- 对话历史：提取任务目标、问题解决过程、技术讨论
- Git 变更：`git status`, `git diff`, `git log`

#### 相关文档更新逻辑

**README.md 更新规则：**
- 新增功能/页面 → 更新"✨ 特性"章节
- 修改部署方式 → 更新部署章节

**其他文档更新规则：**
- `docs/architecture.md`：架构变更时更新
- `docs/DESIGN_SYSTEM.md`：设计系统变更时更新
- `docs/feishu-api-usage.md`：飞书 API 使用变更时更新
- `docs/bitable-import-guide.md`：导入流程变更时更新

### 三、Token 优化策略

1. **不使用 CLAUDE.md**：避免每次对话自动加载
2. **按需加载**：只在需要时读取文档
3. **精简摘要**：工作日志末尾添加 100-150 tokens 的快速摘要
4. **分层加载**：
   - 新对话：只读快速摘要（100-150 tokens）
   - 需要详细信息：读完整工作日志（2000+ tokens）
   - 需要完整需求：读需求文档（3000+ tokens）

### 四、关键文件

**需要创建的 Skill 文件：**
1. `~/.claude/skills/context-keeper/SKILL.md`

**需要生成的文档（操作 1 - 保存）：**
- `docs/plans/YYYY-MM-DD-HHmm-[功能模块]需求.md`
- `docs/worklog/YYYY-MM-DD-HHmm-[功能模块].md`

**需要修改的文件（操作 1 - 保存）：**
- 根据对话内容和 git 变更，可能需要更新：
  - `README.md`
  - `docs/architecture.md`
  - `docs/DESIGN_SYSTEM.md`
  - 其他相关文档

## 实现步骤

### Phase 1: 创建 `context-keeper` Skill

1. 创建 skill 目录结构：`~/.claude/skills/context-keeper/`
2. 编写 SKILL.md，包含：
   - Frontmatter（name, description）
   - 工具介绍（简要说明功能和使用场景）
   - 交互菜单设计（展示选项 1 和 2）
   - 操作 1：保存上下文
     - 对话历史分析方法
     - Git 变更分析方法
     - 需求文档生成模板
     - 工作日志生成模板（包含快速摘要）
     - 文档更新逻辑
     - 支持同一对话多次保存（文件名包含时间戳）
   - 操作 2：加载上下文
     - 查找最新工作日志的方法
     - 读取快速摘要的方法
     - 展示上下文的格式
   - 示例交互流程

### Phase 2: 测试和验证

1. 在当前对话中测试 `/context-keeper` 命令
2. 测试菜单展示和用户选择（输入 1 或 2）
3. 测试保存功能：
   - 验证生成的文档格式和内容
   - 测试同一对话多次保存（文件名时间戳）
4. 开启新对话测试加载功能
5. 验证 token 消耗是否符合预期（< 200 tokens）
6. 根据测试结果调整和优化

## 验证标准

### 功能验证
- [ ] `/context-keeper` 能够正确展示菜单和工具介绍
- [ ] 用户输入 1 或 2 能够正确触发对应操作
- [ ] 操作 1（保存）能够正确分析对话历史和 git 变更
- [ ] 生成的需求文档格式正确，内容完整
- [ ] 生成的工作日志格式正确，包含快速摘要
- [ ] 文件名包含时间戳（HHmm），支持同一对话多次保存
- [ ] 相关文档（README 等）正确更新
- [ ] 操作 2（加载）能够找到最新的工作日志
- [ ] 操作 2（加载）只读取快速摘要，不加载完整文档
- [ ] 上下文展示清晰简洁

### Token 优化验证
- [ ] 新对话使用 `/context` 时，token 消耗 < 200
- [ ] 快速摘要章节 token 消耗在 100-150 之间
- [ ] 不使用 CLAUDE.md，避免自动加载

### 内容质量验证
- [ ] 需求文档准确反映用户需求
- [ ] 工作日志准确记录实现细节和问题
- [ ] 快速摘要简洁明了，包含关键信息
- [ ] 文档更新准确且必要

## 注意事项

1. **对话历史分析**：需要智能提取关键信息，避免遗漏重要内容
2. **功能模块命名**：从对话内容中自动推断，确保命名准确
3. **Git 变更分析**：正确分类文件类型，生成准确的文件清单
4. **文档更新判断**：只更新必要的文档，避免无关修改
5. **快速摘要生成**：确保摘要简洁且包含关键信息
6. **时间戳格式**：使用 HHmm 格式（如 1430），支持同一天多次保存
7. **菜单交互**：清晰展示工具介绍和操作选项，用户只需输入 1 或 2
8. **错误处理**：如果找不到工作日志或 git 信息，给出友好提示

## 参考资料

- 现有工作日志格式：`docs/worklog/2026-04-03-solutions页面导航栏添加与主题适配.md`
- 现有需求文档格式：`docs/plans/2026-03-30-最佳实践三层级页面需求.md`
- README.md 结构
- Skill 创建指南：`superpowers:writing-skills`
