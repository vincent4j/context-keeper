---
name: context-keeper
description: 保存进度、续接上下文、检索经验
---

# Context Keeper — 对话上下文管理工具

帮助你在多个对话之间保持项目上下文的连续性。支持保存当前对话的工作成果、在新对话中继续上次进度，也支持按当前问题查找历史相似经验。

## 交互菜单

当用户只调用 `/context-keeper` 或 `context keeper`，且没有说明具体动作时，展示以下菜单：

```
Context Keeper - 保存进度、延续上下文、查找历史经验的项目记忆助手

功能：
1. 保存本次上下文 — 生成需求文档、工作日志，并更新项目记忆
2. 继续上次进度 — 读取最近工作摘要和近期经验
3. 查找相似经验 — 基于当前会话或用户描述检索历史经验

也可以直接输入：
- context keeper 保存
- context keeper 继续
- context keeper 查找

请选择操作（输入 1-3）：
```

等待用户输入，然后执行对应操作。

直接命令映射：

- `context keeper 保存` / `context-keeper 保存`：直接执行操作 1。
- `context keeper 继续` / `context-keeper 继续`：直接执行操作 2。
- `context keeper 查找` / `context-keeper 查找` / “我记得这个问题以前遇到过，帮我查一下”：直接执行操作 3。

---

## 操作 1：保存本次上下文

### 步骤

1. **分析信息来源**
   - 回顾整个对话历史：提取用户需求、技术决策、解决的问题
   - 运行 `git status`、`git diff HEAD` 获取实际变更（不查询 git log）
   - **判断任务复杂度**（用于确定复盘深度）：
     - **简单任务**：小改动、文档更新、简单 bug 修复、单文件修改
     - **复杂任务**：新功能开发、架构调整、多文件重构、疑难 bug、技术选型

2. **推断功能模块名称**
   - 从对话内容中自动推断，例如 "Solutions页面导航栏添加"
   - 用于文件命名，应简洁准确

3. **生成文件名**
   - 格式：`YYYY-MM-DD-[功能模块]`
   - 示例：`2026-04-03-Solutions页面导航栏添加`
   - **同一对话多次保存**：覆盖同一文件（更新内容即可）
   - **文件名冲突**（不同对话同天同模块）：在末尾加序号，如 `...-2.md`、`...-3.md`

4. **生成需求文档**（可跳过）→ `docs/plans/YYYY-MM-DD-[功能模块]需求.md`

   **跳过条件**：任务类型为 `refactor`、`bugfix`、`config`，或本次工作是纯维护/文档整理，没有新的功能需求可描述时，跳过此步骤。
   
   **生成条件**：任务类型为 `feature` 或 `research`，或有明确的产品/功能需求需要记录时，生成需求文档。

   ```markdown
   # [功能模块] 需求文档

   **创建日期：** YYYY-MM-DD
   **项目：** [项目名称]
   **功能模块：** [功能模块名称]

   ## 一、需求概述
   ## 二、功能结构
   ## 三、数据来源
   ## 四、技术要求
   ## 五、设计要求
   ## 六、验证标准
   ## 七、参考资料
   ```

5. **生成工作日志** → `docs/worklog/YYYY-MM-DD-[功能模块].md`

   ```markdown
   # 工作日志 - YYYY-MM-DD

   ## 项目：[项目名称] - [功能模块]

   ## 一、任务概述
   ## 二、完成的工作
   ## 三、遇到的问题和解决方案
   ## 四、本次对话复盘
   
   [根据任务复杂度填充内容：
   - 简单任务：1-2 段简要复盘，说明遇到了什么问题（如果有）、如何解决、有什么经验
   - 复杂任务：结构化复盘，包含问题识别、根因分析、解决过程、经验教训、预防措施等子章节]

   ## 五、复盘分级

   ### 普通复盘
   [只适合作为本次过程记录的内容]

   ### 重要经验
   [后续同类任务应该主动检索并提醒的经验]

   ### 合同候选
   [可能应由 PRD / 合同系统长期固化的强约束；如果没有则写“无”]
   
   ## 六、创建/修改的文件清单
   ## 七、技术栈/技术决策
   ## 八、下一步工作/总结
   ## 九、备注（可选）

   ---

   ## 快速摘要（用于下次对话）

   **类型：** [feature|bugfix|refactor|research|config] | 项目：[项目名]
   **完成：** [一句话描述完成的工作]
   **问题：** [遇到了什么问题 + 怎么解决的（事实描述），如果没有则省略]
   **经验：** [学到了什么 + 下次怎么做（经验提炼），如果没有则省略]
   **下一步：** [下一步计划]
   **文件：** [新增/修改的关键文件列表]
   ```

   快速摘要控制在 100–150 个上下文 token，是下次对话加载的核心内容。
   
   类型定义：`feature`（新功能）/ `bugfix`（问题修复）/ `refactor`（重构优化）/ `research`（调研探索）/ `config`（配置环境）
   
   **生成工作日志时的指引：**
   - 根据步骤 1 判断的任务复杂度，选择合适的复盘深度
   - **简单任务复盘**：1-2 段文字，重点突出问题和经验
   - **复杂任务复盘**：使用结构化章节（4.1 问题识别、4.2 根因分析、4.3 解决过程、4.4 经验教训、4.5 预防措施）
   - 重点关注"为什么"和"如何改进"
   - 如果本次对话很顺利没有问题，也要总结成功的经验
   - 复盘内容应该对未来的工作有指导意义
   - **快速摘要中的"问题"和"经验"要区分**：
     - "问题"：事实描述，遇到了什么 + 怎么解决的
     - "经验"：经验提炼，学到了什么 + 下次怎么做

6. **更新项目记忆** → `docs/memory-keeper.md`
   
   每次保存工作日志后，自动更新项目记忆文件。`memory-keeper.md` 是历史经验和合同候选的检索入口，不是硬约束来源；如果项目安装了 PRD / 合同类工具，合同候选可作为后续提炼输入。
   
   **索引条目格式：**
   ```markdown
   ## YYYY-MM-DD - [功能模块] `[类型]`
   - **模块：** [模块名或功能域，例如 xhs-local-collector]
   - **触发词：** [用户需求、报错、字段、接口、文件名、模型名等关键词，逗号分隔]
   - **任务：** [一句话描述任务]
   - **关键经验：** [最重要的 1-2 条经验教训，合并为一行，分号分隔]
   - **合同候选：** [可能需要长期固化的强约束；没有则写“无”]
   - **详见：** [worklog/YYYY-MM-DD-[功能模块].md](worklog/YYYY-MM-DD-[功能模块].md)
   ```
   
   如果 `docs/memory-keeper.md` 不存在，则创建它，并添加文件头：
   
   ```markdown
   # 项目记忆索引

   ## 主题摘要（按类型）

   ---

   ## 时间线（最新在前）

   ---
   
   ```
   
   **主题摘要区块维护规则：**
   - 每次保存后，更新"主题摘要"区块中对应类型的条目
   - 格式：`**[类型]（N条）：** [最近3条经验关键词或触发词，逗号分隔]`
   - 整个主题摘要区块控制在 150 个上下文 token 以内
   - 只保留有记录的类型，没有记录的类型不显示
   
   示例主题摘要区块：
   ```markdown
   ## 主题摘要（按类型）

   **bugfix（3条）：** 连接池泄漏检查 finally 块，JWT 时区问题，CSS 层叠顺序
   **refactor（2条）：** 模板区分指引和内容，字段设计明确侧重点
   **feature（5条）：** 认证流程先设计接口再实现，组件拆分粒度适中
   ```
   
   **时间线区块：** 新条目追加到"时间线"区块顶部（最新在前）。

7. **按需更新相关文档**（只更新真正需要的）
   - `README.md`：新增功能/页面 → 更新"特性"章节；修改部署方式 → 更新部署章节
   - `docs/architecture.md`：架构有变更时
   - `docs/DESIGN_SYSTEM.md`：设计系统有变更时
   - 其他项目文档：视实际变更内容决定

8. **输出保存完成摘要**

   保存完文件后，必须在对话窗口输出一段"值得回看"摘要，帮助用户知道 worklog / memory 里有哪些重要信息，而不是只告诉用户文件已生成。

   **筛选规则：**
   - 优先来自工作日志的"四、本次对话复盘"、"五、复盘分级"、快速摘要和 `memory-keeper.md` 新增条目。
   - 先列候选，再按以下维度判断价值：可复用性、踩坑风险、认知增量、决策价值、约束强度。
   - 只输出高价值候选：能指导下次行动、避免重复踩坑、解释关键取舍，或接近长期规则的信息。
   - 普通流水账、文件列表、已完成事项、低价值感想不放入重点摘要，除非它们解释了关键决策。
   - 最多输出 3 条"重要经验"，最多输出 2 条"合同候选 / 风险提醒"；数量是上限，不要为了凑数输出弱信息。
   - 如果没有高价值经验，明确写"本次没有提炼出需要特别回看的经验"。

   **压缩规则：**
   - 每条重要经验只写"行动结论 + 价值理由"，不复述排查过程。
   - 行动结论要能直接指导下一次同类任务，例如"先区分 A/B/C，再决定改哪里"。
   - 价值理由说明为什么值得用户点开看，例如"避免下次只看最终结果而盲修"。
   - 合同候选只写强约束或风险边界，不输出"建议优化"这类软提醒。
   - 细节、证据和完整过程留在工作日志 / memory 链接里。

   **输出格式：**
   ```text
   已保存上下文。

   值得回看的重点：
   1. ...
      价值：...
   2. ...
      价值：...

   合同候选 / 风险提醒：
   - ...

   详细内容：
   - 工作日志：[docs/worklog/...](...)
   - 项目记忆：[docs/memory-keeper.md](...)
   ```

   摘要要短，目标是让用户判断要不要点开完整 MD，而不是复述 MD。

9. **询问是否提交代码**：所有文档保存完成且输出重点摘要后，询问用户：

   ```
   文档已保存完毕。是否需要提交代码并推送到远程？
   1 - 是，提交并推送
   2 - 否，跳过
   ```

   - 用户选择 `1`：运行 `git add -A`，根据本次工作内容生成一条简洁的 commit message，执行 `git commit` 和 `git push`，完成后告知用户结果
   - 用户选择 `2`：跳过，结束流程

---

## 操作 2：继续上次进度

### 步骤

1. **读取最近 3 次工作摘要**
   - 查找 `docs/worklog/` 目录下最新的 3 个工作日志文件（按文件名排序，取最后 3 个）
   - 每个文件只读取"快速摘要"章节（约 100–150 个上下文 token/个，不加载完整文档）
   - 如果不足 3 个，读取全部

2. **读取近期经验**
   - 如果 `docs/memory-keeper.md` 存在，先读取顶部的"主题摘要"区块（约 150 个上下文 token）
   - 根据用户触发语句推断当前任务类型（feature/bugfix/refactor/research/config）
   - 如果能推断类型：优先展示同类型最近 3 条 + 不限类型最近 2 条（共 5 条）
   - 如果无法推断：展示最近 5 条（不限类型）

3. 以简洁格式告知用户：

   ```
   最近工作摘要：

   [1] YYYY-MM-DD - [类型] - [功能模块]
   完成：...
   下一步：...

   [2] YYYY-MM-DD - [类型] - [功能模块]
   完成：...
   下一步：...

   [3] YYYY-MM-DD - [类型] - [功能模块]
   完成：...
   下一步：...

   近期经验（最近 5 条）：
   - [类型] YYYY-MM-DD - [功能模块]：[关键经验]
   - ...

   需要查看某次的完整工作日志或需求文档吗？
   ```
   
   如果 `memory-keeper.md` 不存在或为空，则跳过"近期经验"部分。

4. 根据用户回应决定是否加载更多内容

---

## 操作 3：查找相似经验

### 步骤

1. **判断是否已有当前会话上下文**
   - 如果当前会话已经包含用户需求、报错、测试失败、文件路径、模块名、字段名、接口名、模型名或实现过程，直接从这些内容自动提取检索关键词，不要求用户手动输入关键词。
   - 如果当前会话几乎没有可用上下文（例如新会话刚开始只输入“context keeper 查找”），先提示用户：

   ```text
   请简单描述要查的问题，或者粘贴报错/需求。我会据此检索 docs/memory-keeper.md。
   ```

2. **自动提取关键词**
   - 优先提取：模块名、业务名词、错误码/报错、字段名、接口名、文件名、类名/函数名、模型名、测试名、合同 ID。
   - 从当前会话中提取时，先覆盖最近用户需求和当前卡点，再补充已触碰文件和已运行命令中的关键词。
   - 提取后先简短说明：

   ```text
   我会基于当前会话自动检索这些线索：关键词1, 关键词2, 关键词3
   ```

3. **检索 `docs/memory-keeper.md`**
   - 如果 `docs/memory-keeper.md` 不存在，提示尚无项目记忆，建议先执行保存操作。
   - 使用关键词在 `docs/memory-keeper.md` 的全文件范围内搜索，覆盖历史条目但不默认全文加载。
   - 优先读取命中的完整条目；只有条目不足以判断时，才按 `详见` 链接读取对应 worklog 的相关段落。

4. **输出命中结果**

   ```text
   命中 N 条相似经验：

   1. YYYY-MM-DD - [标题]
   模块：...
   命中线索：...
   适用经验：...
   合同候选：...
   详见：...

   本轮建议：
   - ...
   - ...
   ```

5. **生效边界**
   - `memory-keeper.md` 命中的内容是历史经验和合同候选，用来提醒排查方向和避免重复踩坑。
   - Context Keeper 单独安装时，不把这些内容称为“生效合同”或“硬约束”。
   - 如果项目另有 PRD / 合同系统，应把“合同候选”交给对应系统提炼；真正的硬约束以项目生效合同为准。

---

## 上下文优化原则

- **不使用 CLAUDE.md**：避免每次对话自动加载大量内容
- **分层加载**：
  - 继续上次进度默认加载最近 3 次快速摘要（约 450 个上下文 token）+ 主题摘要区块（约 150 个上下文 token）+ 近期经验 5 条（约 150 个上下文 token）≈ 750 个上下文 token
  - 按需加载完整工作日志（约 2000 个上下文 token）
  - 按需加载需求文档（约 3000 个上下文 token）
- **项目记忆索引**：
  - 主题摘要区块提供类型级别的快速概览（约 150 个上下文 token）
  - 按类型过滤后只加载相关经验，节省 50-80% 上下文 token
  - 查找相似经验时，全文件范围搜索 `memory-keeper.md`，只读取命中条目，不受最近条目限制

---

## 错误处理

- 找不到 `docs/worklog/` 目录或为空 → 提示用户尚无工作日志，建议先执行保存操作
- 找不到 `docs/memory-keeper.md` → 查找相似经验时提示尚无项目记忆，建议先执行保存操作
- git 命令失败（非 git 仓库等）→ 跳过 git 分析，仅基于对话历史生成文档，并在文档中注明
- 功能模块名称不确定 → 询问用户确认后再生成
