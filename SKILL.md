---
name: context-keeper
description: Use when the user wants to save the current session's work context, generate a worklog or requirements doc, or load recent project context at the start of a new conversation. Trigger on /context-keeper, "保存上下文", "加载上下文", "记录工作日志", "session结束", "保存会话", or when the user asks to summarize what was done this session.
---

# Context Keeper — 对话上下文管理工具

帮助你在多个对话之间保持项目上下文的连续性。支持保存当前对话的工作成果，也支持在新对话中快速加载上次的进度摘要。

## 交互菜单

当用户调用 `/context-keeper` 时，展示以下菜单：

```
Context Keeper - 对话上下文管理工具

功能：
1. 保存上下文 — 生成需求文档、工作日志，并更新相关文档
2. 加载上下文 — 读取最近工作日志的快速摘要，了解项目当前状态

请选择操作（输入 1 或 2）：
```

等待用户输入，然后执行对应操作。

---

## 操作 1：保存上下文

### 步骤

1. **分析信息来源**
   - 回顾整个对话历史：提取用户需求、技术决策、解决的问题
   - 运行 `git status`、`git diff HEAD` 获取实际变更（不查询 git log）

2. **推断功能模块名称**
   - 从对话内容中自动推断，例如 "Solutions页面导航栏添加"
   - 用于文件命名，应简洁准确

3. **生成文件名**
   - 格式：`YYYY-MM-DD-[功能模块]`
   - 示例：`2026-04-03-Solutions页面导航栏添加`
   - **同一对话多次保存**：覆盖同一文件（更新内容即可）
   - **文件名冲突**（不同对话同天同模块）：在末尾加序号，如 `...-2.md`、`...-3.md`

4. **生成需求文档** → `docs/plans/YYYY-MM-DD-HHmm-[功能模块]需求.md`

   ```markdown
   # [功能模块] 需求文档

   **创建日期：** YYYY-MM-DD
   **项目：** [项目名称]
   **功能模块：** [功能模块名称]

   ## 一、需求概述
   ## 二、功能结构
   ## 三、数据来源
   ## 四、技术要求
   ## 五、设计要求
   ## 六、验证标准
   ## 七、参考资料
   ```

5. **生成工作日志** → `docs/worklog/YYYY-MM-DD-HHmm-[功能模块].md`

   ```markdown
   # 工作日志 - YYYY-MM-DD

   ## 项目：[项目名称] - [功能模块]

   ## 一、任务概述
   ## 二、完成的工作
   ## 三、遇到的问题和解决方案
   ## 四、创建/修改的文件清单
   ## 五、技术栈/技术决策
   ## 六、下一步工作/总结
   ## 七、备注（可选）

   ---

   ## 快速摘要（用于下次对话）

   **完成：** [一句话描述完成的工作]
   **问题：** [遇到的主要问题及解决方案，如果没有则省略]
   **下一步：** [下一步计划]
   **文件：** [新增/修改的关键文件列表]
   ```

   快速摘要控制在 100–150 tokens，是下次对话加载的核心内容。

6. **按需更新相关文档**（只更新真正需要的）
   - `README.md`：新增功能/页面 → 更新"特性"章节；修改部署方式 → 更新部署章节
   - `docs/architecture.md`：架构有变更时
   - `docs/DESIGN_SYSTEM.md`：设计系统有变更时
   - 其他项目文档：视实际变更内容决定

7. **通知用户**：简要告知生成了哪些文件

8. **询问是否提交代码**：所有文档保存完成后，询问用户：

   ```
   文档已保存完毕。是否需要提交代码并推送到远程？
   1 - 是，提交并推送
   2 - 否，跳过
   ```

   - 用户选择 `1`：运行 `git add -A`，根据本次工作内容生成一条简洁的 commit message，执行 `git commit` 和 `git push`，完成后告知用户结果
   - 用户选择 `2`：跳过，结束流程

---

## 操作 2：加载上下文

### 步骤

1. 查找 `docs/worklog/` 目录下最新的工作日志文件（按文件名排序，取最后一个）
2. 只读取"快速摘要"章节（约 100–150 tokens，不加载完整文档）
3. 以简洁格式告知用户：

   ```
   上次工作摘要（来自 docs/worklog/YYYY-MM-DD-HHmm-xxx.md）：

   完成：...
   问题：...
   下一步：...
   文件：...

   需要查看完整工作日志或需求文档吗？
   ```

4. 根据用户回应决定是否加载更多内容

---

## Token 优化原则

- **不使用 CLAUDE.md**：避免每次对话自动加载大量内容
- **分层加载**：
  - 新对话默认只读快速摘要（~150 tokens）
  - 按需加载完整工作日志（~2000 tokens）
  - 按需加载需求文档（~3000 tokens）

---

## 错误处理

- 找不到 `docs/worklog/` 目录或为空 → 提示用户尚无工作日志，建议先执行保存操作
- git 命令失败（非 git 仓库等）→ 跳过 git 分析，仅基于对话历史生成文档，并在文档中注明
- 功能模块名称不确定 → 询问用户确认后再生成
