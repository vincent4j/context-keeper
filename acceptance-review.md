# Context Keeper 本会话验收对照

本文件是交付对照表，不参与每次 Skill 调用。2026-09-16 更新。

依据：本会话原始用户消息、最终对齐方案及后续修正。撤回的目录名称、每次选方案都查历史、保存后永不更新等旧建议不作为验收要求。实现和平台运行时验证分开记账。

## 上次审查的 12 项

| 项 | 最终处理 | 证据与状态 |
|---|---|---|
| 1 失败重试触发遗漏 | 安装入口补本轮失败准备重试/换路线且证据不足，更新两个用户级入口 | 源码与入口读回通过；运行时见第 12 项 |
| 2 原始历史检索 | include-current 同时作用于数据库和文件；数据库零命中回退；Claude 缺少项目身份不纳入结果 | `test_database_lag_and_include_current`、`test_claude_unknown_project_is_excluded` 通过 |
| 3 历史保护 | 首次创建/检查记录前保存旧计划/日志指纹，save-report 检查改写、删除；无基线不能宣称保存通过 | `test_old_plan_edit_and_deletion_block_delivery_without_git` 通过；不是文件系统写入拦截 |
| 4 经验状态及替代 | 已验证优先；待验证标明；已替代排除；替代链接必须有效且无循环 | `test_verified_rank_dedup_index_trigger_and_resume`、`test_replacement_requires_real_current_target` 通过 |
| 5 续接进化经验 | 有任务关键词时加入项目/用户级经验，与时间线共用五条预算 | 同上及 `test_kind_budget_includes_evolution` 通过 |
| 6 待办状态 | 明确状态字段；续接不展示已完成/已取消事项 | `test_pending_state_and_completed_filter` 通过 |
| 7 保存覆盖 | 检查真实链接而非文件名文本；检查计划和经验正文断链及经验入口 | `test_coverage_real_links_and_evolution_entry`、`test_save_rejects_missing_index_entry` 通过 |
| 8 重复经验 | 晋升副本按编号与规范化证据去重；同标题重复与跨范围编号冲突报告 | `test_same_theme_duplicates_reported`、晋升及去重测试通过；语义同义主题仍由 Agent 判断，不建语义系统 |
| 9 Plan 质量 | 真实需求、范围、实施计划、验证方式、待确认五个非空章节；本会话计划纳入 save-report | `test_plan_quality_and_invalid_date` 通过；字段校验不冒充需求语义验收 |
| 10 日期格式 | 拒绝不符合 YYYY-MM-DD 的日期及无效日历日期 | 同上通过 |
| 11 真实案例闭环 | 两个案例的历史原文已找到；按真实失败类型建立隔离双会话用例，验证沉淀、续接、复用反馈、历史不改写和 coverage | `test_two_session_experience_and_delivery_loop` 通过。脚本闭环已验证；本次新增真实 Agent 运行，结果见下表 |
| 12 新任务自动触发 | 两个平台用户级入口已配置；安装器输出改为 configured_agents，避免把配置成功写成运行验证 | Codex 本次四类用例通过；Claude Code 当前 MiniMax 后端自动沉淀失败，不能记为全部完成 |

## 最终需求清单

| 约定 | 实现或证据 |
|---|---|
| 保留保存、继续、查找三入口 | SKILL.md 操作路由 |
| 原文、摘要、推断分开；不迎合记忆；零命中不推断从未发生 | SKILL.md、search.md、零命中测试及实际原文回查 |
| 关键词由上下文提取 | 入口及 search.md；Codex 实测自行提取关键词；其他环境见运行报告 |
| 普通新需求不检索；历史信号、失败重试、推翻旧决策才探测 | 全局短入口及 SKILL.md；普通需求对照两端均未触发；Codex 失败后检索通过；推翻旧决策未单独实测 |
| 同问题不重复搜索，新错误/证据/路线变化除外 | 全局短入口及操作说明 |
| 自动沉淀三类事件，原有主题优先更新 | SKILL.md、save.md；字段和同标题重复检查，不假装自动理解所有同义主题 |
| 要求、运行验证、猜测分开；不把用户纠正当技术验证 | save.md 事实类型说明 |
| 再次失败检查未记录、未命中、未采用、不适用 | save.md；不自动堆叠规则 |
| 默认可见根目录 context-keeper，可指定位置，第一次提示一次 | init 测试通过；配置记忆及显式迁移测试通过 |
| memory-keeper、plans、worklogs、evolution/index 固定名称 | 初始化及入口测试通过 |
| 计划/日志日期加中文；经验文件允许中文 | record-path 格式校验；Skill 模板 |
| 同会话同主题更新，不同独立主题可拆文件，跨会话新增 | record-path/record-guard/指纹回归测试 |
| A 改 B 不回写旧会话的 A；新记录链接旧记录 | save.md；指纹校验防止交付前旧记录被改 |
| plans 保存真实需求及受约束实施计划 | 模板及 Plan 检查；不推断新增用户要求 |
| 旧 docs 兼容读取，不自动迁移和改写 | 旧目录测试通过 |
| 经验分主题、索引只存主题/触发/范围/链接 | save.md；索引触发词可参与检索 |
| 沉淀、实际采用、验证有效分别反馈；不只写日志 | save-report 与跨会话闭环测试；Codex 实测有沉淀与复用反馈；Claude 显式保存有反馈 |
| 项目经验隔离；明确批准后才晋升用户级 | 晋升批准测试；相对证据链接晋升后保持有效 |
| 用户级安装作用于各项目，项目级只在指定项目 | 安装测试通过 |
| 两级并存项目优先，一次执行 | Codex 两级并存测试中仅执行一次；Claude 自动路由未通过，不据此承诺配置优先级普遍正确 |
| 安装、更新、卸载只管理自己的区块 | 安装器回归测试通过 |
| 其他 Agent 不冒称已验证 | install.md 明确限制 |
| 三份短摘要、五条经验、类型 3+2、缺摘要降级 | 续接及预算测试通过 |
| 搜索最多五候选，经验最多三候选，只展开一至两段 | 搜索脚本限制与 search.md |
| coverage 默认简短，历史缺口按需展开 | 一行输出测试及真实项目回放 |
| 不自动增加提交/推送权限 | Skill 明确；本轮未提交/推送 |
| 不建新数据库、向量库、后台监听、自动训练或自改 Skill | 无上述组件；读取既有历史库，经验使用 Markdown |
| 不修改外部项目功能，独立运行 | 本轮仅本仓库和已授权用户级入口有写入；外部项目只读 |

## 本次检查结果

- 52 项自动测试通过，覆盖迁移证据指向、无效配置、安装目录归属、原始会话半行写入及用户级索引保护。
- 使用 `-W error::ResourceWarning` 重跑通过。
- Skill 结构校验通过，Git diff 格式检查通过。
- 两个全局入口分别只有一个受控区块，包含失败重试条件。
- 真实项目只读回放：resume 约 0.063 秒/24 行；search 约 0.068 秒/6 行；coverage 约 0.052 秒/1 行；整体复盘原文约 1.06 秒/9 行。单次观测，不作为性能保证。
- coverage 返回 1 是真实旧记录存在缺口，未批量修补，也未把非零记为通过。
- 没有报告未经测量的 Token 节省百分比。

## 本次真实 Agent 运行验收

| 场景 | Codex / gpt-6-astra | Claude Code / MiniMax-M3 |
|---|---|---|
| 普通文字修改不查历史 | 通过 | 通过 |
| 用户纠正后自动沉淀并告知 | 通过 | 失败；禁用其他 hooks 的对照仍失败 |
| 下一独立任务拒绝错误记忆并复用经验 | 通过，执行受限搜索 | 部分通过：引用原文并保持历史不变，但绕过受限检索，读取过多文件 |
| 失败后换路线前查历史 | 通过 | 未单独运行，不记为通过 |
| 显式调用保存 | 已包含在保存链路 | 通过，coverage/save-report 均通过 |

完整条件、证据和限制见 [behavior-verification.md](behavior-verification.md)。这是有限用例的实际结果，不代表所有模型、所有 Agent 或未来任务必然遵循规则。

## 本轮补齐

- 自动沉淀入口明确先调用 Skill 并读取保存路由，不能只改业务文件就结束。
- 已有权威文档时仅记录变化并链接，不按任务类型机械创建计划。
- 迁移可能重定向历史外部相对链接时停止，保持历史与配置不变；不偷偷改写旧记录。
- 原始会话最终方案重新逐项对照；源码、测试、运行结果分开报告。

## 尚未达成

跨 Agent 自动行为尚未全部验收通过。当前 Claude Code/MiniMax 后端的自动沉淀失败，历史检索预算也未遵守。规则存在且可读取、显式调用能工作，仍不足以证明自动执行可靠。尚无证据表明继续增加规则能解决；本轮不引入逐消息 hooks、全局模型变更或后台系统。

## 用户确认的收尾边界

用户明确要求不使用复杂 hooks，并决定原生 Claude 今天不验收，等实际使用时再提出验收和测试。因此该项为用户决定延期，不再在本轮探测渠道、调用模型或要求登录。延期不等于验收通过，也不消除 MiniMax 已发现的问题。

本轮交付保留现有轻量入口、脚本、测试和说明；后续触发条件是用户实际使用时明确要求验收。届时复用同组隔离案例，检查实际模型、自动沉淀、跨会话复用和读取预算。没有新增提交、推送或发布授权。

## 提交前最后审查

本轮审查修复：

- 卸载拒绝删除正在使用的真实源码目录；安装拒绝覆盖归属不明的同名目录；已安装软链接仍可安全解除。
- 无效目录配置返回失败，不静默创建另一套默认记录。
- 迁移检查扩展到内联图片、JSON 等证据链接及内部绝对路径；不安全则保持原目录和配置不变；源目标不能相互包含。
- 原始会话尾部存在未写完的 JSON 时保留前面的有效消息，不因一行解析失败丢弃整份记录。
- 晋升用户级经验禁止使用 index.md 或非 Markdown 文件名，避免覆盖索引或写入不可检索文件。
- 恢复原探测脚本执行权限；历史结果区分数据库 ordinal 与文件 line，避免误导原文定位。

52 项本地测试通过。原生 Claude 验收仍按用户决定延期，本次不调用模型，不增加 hooks。提交范围包括新增安装说明、安装器、两份验收报告及测试文件；仅检查已跟踪差异会漏掉这些未跟踪文件。
